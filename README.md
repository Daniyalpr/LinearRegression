# LinearRegression
A linear regression model.

# Using
Create a Model object and pass the number of features as input.
The model doesn't support batching.
The shape of labels should be (n, ) (a vector) which n is number of samples.
The shape of train data should be (n, d) which d is number of features.
